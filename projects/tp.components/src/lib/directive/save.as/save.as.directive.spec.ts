import { SaveAsDirective } from './save.as.directive';

xdescribe('SaveAsDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveAsDirective();
    expect(directive).toBeTruthy();
  });
});
